import React from 'react';
import { Grid } from '@material-ui/core';

const Maps = () => {
	return (
		<Grid container spacing={8}>
			<Grid item xs={4}>
				Maps
			</Grid>
		</Grid>
	);
};

export default Maps;
